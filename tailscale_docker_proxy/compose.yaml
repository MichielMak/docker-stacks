services:
  tailscale_docker_proxy:
    container_name: tailscale_docker_proxy
    image: almeidapaulopt/tsdproxy:1.4.7-amd64@sha256:bf0f812f66d3e0f797568be7914d388c6647cdaf5fbd8138e2866c44d986c346
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - tsdpdatadir:/data
      - ${DOCKER_DATA_DIR}/tsdproxy/config:/config
    restart: unless-stopped
    networks:
      - npm
#    ports:
#      - 8080:8080
volumes:
  tsdpdatadir: null
networks:
  npm:
    external: true